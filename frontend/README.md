# TraceID Frontend

React + Vite interface for TraceID.

## Quick start

```bash
npm install
npm run dev
```

Opens at http://localhost:5173  
API calls are proxied to `http://localhost:8000` via Vite.

## Build

```bash
npm run build
npm run preview
```
